description: Not Found
content:
  application/vnd.api+json:
    schema:
      type: object
      required:
        - responses
      properties:
        errors:
          type: array
          items:
            $ref: "#/components/schemas/NotFound"
